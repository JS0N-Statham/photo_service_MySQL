			
			Я точно помню где я создавал имя пользователя (root) и пароль к нему (1111). 
		Было ли это при запуске MySQL Installer - Community, MySQL Workbench или MySQL Server. 

1) на Windows в пуске появляется MySQL Commander Line client unicode, это консоль mysql>

			но если ее нет, то вот такой есть способ запустить её:
					запускаем cmd

2)  >cd C:\*путь к папке bin в mysql server*\bin>bin   #заходим в директорию MySQL и пишем команду: bin чтобы в консоле было: mysql>, а не: C:\Program Files\MySQL\MySQL Server 8.0>


3) mysql> mysql -u root -p  #root - имя пользователя, -p - парооль


4)  Enter password: ****   #Вводим пароль 


5) mysql> source C:/Users/MASTER/Desktop/project/photo_service/object4.sql;    #/Users/MASTER/Desktop/project/photo_service/ - путь к скрипту sql (его запуск)


В скрипте object4.sql в начале срабатывает команда которая дропает все данные в БД {DROP DATABASE IF EXISTS object4;}, ее лучше закоментить (или вообще ее лучше удалить) я каждый раз удалял эту бд, чтобы не создавать новую, а очистить старую и начать все с чистого листа
Скрипт object4.sql который создает таблицы в одноименной БД - object4, не обязательно называть object4.sql, как БД. Это используется для простоты понимания





					**************************************************************
					**************************************************************
					*********************** Доп команды **************************
					**************************************************************

					  mysql> create database BD;  # так можно создать БД: BD
  
					  mysql> USE BD; # так мы берем БД: BD в работу, которая будет использоваться


Ребята, я не тупой. Просто не спал двое суток.
